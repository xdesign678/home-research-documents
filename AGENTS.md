# 项目规则

## 小红书内置浏览器使用边界

- 使用内置浏览器访问小红书时，只允许做公开页面的搜索、打开、浏览、阅读和必要的信息记录。
- 禁止执行会改变账号或内容关系的动作，包括点赞、收藏、关注、评论、私信、分享、发布、下单、举报、批量互动。
- 不编写、请求或执行绕过风控的方案；不得使用代理池、账号轮换、验证码绕过、指纹伪装、接口抓包直连、Cookie 注入等手段。
- 操作节奏以低干扰为原则：不并发打开大量页面，不高速连续点击，不无限滚动采集，不长时间重复同一关键词或同一入口。
- 如出现登录、验证码、异常访问、账号安全、滑块验证、频繁访问提示，立即停止自动操作，并把当前页面状态说明给用户。
- 默认不主动登录账号；如确实需要登录，只能由用户明确授权并手动完成，不保存账号、密码、Cookie、Token 或其他凭证。
- 记录信息时只保留任务必要内容，例如标题、公开链接、可见时间、公开指标和观察结论；不采集个人隐私、联系方式或非公开数据。
- 对搜索和浏览结果要保留来源意识：能给出页面链接就给链接，不能核验的数据要写清楚“未核验”。

## 小红书任务执行原则

- 优先使用平台公开搜索和页面可见信息，不把浏览器自动化用于规避平台限制。
- 若任务目标需要大量采集、监控、刷量、养号、互动或规避检测，直接拒绝该部分，只提供合规替代方案。
- 若用户要求“更像真人以避免风控”，只能理解为降低打扰和减少误触发：慢一点、少一点、只读、不并发、遇验证即停。
- 不用“反检测”“养号”“破风控”作为实现目标；实现目标应写成“合规浏览”“低频只读”“人工确认后继续”。

## 协作与交付

- 回答用户时先给结论，再补充必要理由；中文表达保持自然、直接、克制。
- 如果涉及 Git 提交，提交说明使用中文，说明需求、修复或实现思路。
- 如果当前目录不是 Git 仓库，明确说明不能提交或推送。
